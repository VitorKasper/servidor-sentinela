PORT=3000
HOST=0.0.0.0
SESSION_SECRET=sentinela_super_secret_session_key_2026

# MySQL Database Configuration
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_NAME=servidor_sentinela

# Default Admin Credentials (created automatically on first boot if DB is empty)
DEFAULT_ADMIN_NAME=Administrador
DEFAULT_ADMIN_EMAIL=admin@sentinela.local
DEFAULT_ADMIN_PASSWORD=admin123

# Storage path for cloned projects
PROJECTS_STORAGE_PATH=./storage/projects

# Ignorar checagem SSL globalmente (útil em redes corporativas com proxy/firewall SSL inspection)
# IGNORE_SSL=true

